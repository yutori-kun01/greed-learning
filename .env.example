# Copy this file to .env for local development (npm run dev).
# In production these are NOT read from .env — set them as Cloudflare
# Workers secrets instead (see DEPLOY.md): `wrangler secret put NAME`.

# Better Auth
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
# Full public URL of the deployed site (no trailing slash)
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Google OAuth (optional — leave blank to disable "Sign in with Google")
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Stripe (required for paid posts and subscriptions)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Resend (required to actually deliver password-reset and email-change
# confirmation emails — without these, those emails are only logged, not sent)
RESEND_API_KEY=
RESEND_FROM_EMAIL=

# 管理者にするアカウントのメールアドレス。設定しておくと、このアドレスで
# 登録したアカウントだけが管理者になります。未設定の場合は「最初に登録した
# アカウント」が管理者になるため、公開直後に第三者に取られる可能性があります。
ADMIN_EMAIL=

# メール確認を必須にする（true で有効）。有効にすると、登録時に確認メールが
# 送られ、確認を終えるまでログインできません。RESEND_* が未設定の場合は、
# 誰も確認メールを受け取れず全員が締め出されるため、この設定は無視されます。
REQUIRE_EMAIL_VERIFICATION=false

# R2 画像ストレージ（プロフィール画像・ロゴ・記事内画像のアップロードに必須）
# 未設定の場合、アップロードは「画像ストレージが未設定」というエラーになります。
# 取得方法と、バケットの公開設定・CORS設定は DEPLOY.md の「R2（画像アップロード）の設定」を参照してください。
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=greed-learning-assets
# バケットの公開URL（末尾スラッシュなし）。例: https://pub-xxxx.r2.dev や https://assets.your-domain.com
R2_PUBLIC_URL=

# Local-dev only: bypass login and act as an admin without signing in.
# Never set this to true outside development.
ENABLE_DEV_BYPASS=false
